# name: Docker Compose Build CI

# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]

# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: rathalin/quizio
#   USERNAME: rathalin

# jobs:
#   build-and-push-images:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

#       - name: Log in to the Container Registry
#         uses: docker/login-action@v2
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ env.USERNAME }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Export environment variables for Docker Compose
#         run: |
#           echo "GO_ENV=${{ secrets.GO_ENV }}" >> $GITHUB_ENV
#           echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> $GITHUB_ENV
#           echo "POSTGRES_DB=${{ secrets.POSTGRES_DB }}" >> $GITHUB_ENV
#           echo "POSTGRES_HOST=${{ secrets.POSTGRES_HOST }}" >> $GITHUB_ENV
#           echo "POSTGRES_USER=${{ secrets.POSTGRES_USER }}" >> $GITHUB_ENV
#           echo "POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}" >> $GITHUB_ENV
#           echo "OPENAPI_DOCS_USER=${{ secrets.OPENAPI_DOCS_USER }}" >> $GITHUB_ENV
#           echo "OPENAPI_DOCS_PASSWORD=${{ secrets.OPENAPI_DOCS_PASSWORD }}" >> $GITHUB_ENV

#       - name: Set COMPOSE_FILE variable
#         run: echo "COMPOSE_FILE=infra/docker-compose.production.yml" >> $GITHUB_ENV

#       - name: Build and push images with Docker Compose
#         run: |
#           docker compose -f $COMPOSE_FILE build
#           docker compose -f $COMPOSE_FILE push
